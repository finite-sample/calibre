name: CI

on:
  push:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '*.md'
      - 'docs/**'
      - 'examples/**/*.md'
      - '.github/*.md'
      - 'LICENSE'
      - 'citation.cff'
      - 'CHANGELOG.md'
      - 'CLAUDE.md'
  pull_request:
    branches: [ main ]
    paths-ignore:
      - '**.md'
      - '*.md'
      - 'docs/**'
      - 'examples/**/*.md'
      - '.github/*.md'
      - 'LICENSE'
      - 'citation.cff'
      - 'CHANGELOG.md'
      - 'CLAUDE.md'
  workflow_dispatch:

jobs:
  validate-lockfile:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5

    - name: Install uv
      uses: astral-sh/setup-uv@v7.1.4
      with:
        version: "0.9.13"

    - name: Validate lock file is up-to-date
      run: |
        uv lock --locked
        echo "✅ Lock file is consistent with pyproject.toml"

  test:
    needs: validate-lockfile
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Test on Linux with multiple Python versions (main compatibility testing)
          - os: ubuntu-latest
            python-version: "3.11" 
          - os: ubuntu-latest
            python-version: "3.12"
          - os: ubuntu-latest
            python-version: "3.13"
          # Test on other OS with latest stable Python only
          - os: macos-latest
            python-version: "3.11"
          - os: windows-latest
            python-version: "3.11"

    steps:
    - uses: actions/checkout@v5

    - name: Install uv
      uses: astral-sh/setup-uv@v7.1.4
      with:
        version: "0.9.13"
        python-version: ${{ matrix.python-version }}
        enable-cache: true
        cache-dependency-glob: "uv.lock"

    - name: Install dependencies
      run: |
        uv sync --locked --all-extras --dev

    - name: Run ruff linting check (informational)
      continue-on-error: true
      run: |
        uv run ruff check calibre/ tests/

    - name: Run ruff formatting check (informational)
      continue-on-error: true
      run: |
        uv run ruff format --check calibre/ tests/

    - name: Run tests with coverage
      run: |
        uv run pytest --cov=calibre --cov-report=xml --cov-report=term-missing -v


    - name: Build package
      run: |
        uv build

    - name: Test package installation
      run: |
        uv pip install dist/*.whl
        uv run python -c "import calibre; print(f'Successfully imported calibre v{calibre.__version__}')"

